/* 
Ya no se usa var nunca
siempre usaremos const y solo se usará let cuando querramos cambiar el valor de una variable
*/
const nombre = "Tony"
const apellidos = "Stark"

console.log(nombre, apellidos)

if(true){
    const nombre = 'peter'
    console.log(nombre);
}